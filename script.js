const s=[...document.querySelectorAll('.screen')],bar=document.getElementById('bar');let n=0;function show(i){n=Math.max(0,Math.min(i,s.length-1));s.forEach((x,j)=>x.classList.toggle('active',j===n));bar.style.width=(n/(s.length-1)*100)+'%'}document.querySelectorAll('[data-next]').forEach(b=>b.onclick=()=>show(s.findIndex(x=>x.id===b.dataset.next)));let x=0,y=0;document.addEventListener('touchstart',e=>{x=e.changedTouches[0].clientX;y=e.changedTouches[0].clientY},{passive:true});document.addEventListener('touchend',e=>{let dx=e.changedTouches[0].clientX-x,dy=e.changedTouches[0].clientY-y;if(Math.abs(dx)>70&&Math.abs(dx)>Math.abs(dy))show(n+(dx<0?1:-1))},{passive:true});
